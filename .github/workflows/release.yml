name: Build and Release Desktop App

on:
  push:
    tags:
      - 'v*' # Trigger on all version tags (e.g., v1.2.21)

jobs:
  release:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Dependencies
        run: npm install

      - name: Build and Publish (macOS)
        if: matrix.os == 'macos-latest'
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
          VITE_API_BASE: https://hrmsbackend.yoforex.net/api
          VITE_WEB_BASE: https://emptrakr.com
          API_BASE: https://hrmsbackend.yoforex.net/api
          WEB_BASE: https://emptrakr.com
        run: |
          node scripts/write-electron-config.cjs
          npm run build
          npx electron-builder --mac --x64 --arm64 -p always

      - name: Build and Publish (Windows)
        if: matrix.os == 'windows-latest'
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
          VITE_API_BASE: https://hrmsbackend.yoforex.net/api
          VITE_WEB_BASE: https://emptrakr.com
          API_BASE: https://hrmsbackend.yoforex.net/api
          WEB_BASE: https://emptrakr.com
        run: |
          node scripts/write-electron-config.cjs
          npm run build
          npx electron-builder --win -p always

